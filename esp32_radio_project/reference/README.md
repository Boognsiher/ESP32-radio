# Referenz – NICHT direkt übernehmen

Diese Dateien sind der letzte funktionsfähige Stand aus der Vorgänger-
Debugging-Session. Sie dienen nur als Nachschlage-Referenz für Details,
die in `CLAUDE.md` als "bekannte Stolpersteine" erwähnt sind (z.B. die
exakte `Arduino_ST77916`-Konstruktor-Signatur mit Init-Sequenz).

Der neue Code soll **nicht** 1:1 aus diesen Dateien kopiert werden –
Struktur, Aufteilung und Implementierung sollen frisch entworfen werden.
Diese Dateien sind Beleg dafür, dass ein bestimmter technischer Ansatz
(z.B. Konstruktor-Parameter) tatsächlich funktioniert hat, nicht mehr.
